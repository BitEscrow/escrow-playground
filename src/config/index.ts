import presets  from './presets.json'  assert { type : 'json' }
import servers  from './servers.json'  assert { type : 'json' }
import settings from './settings.json' assert { type : 'json' }

export default { presets, servers, settings }
