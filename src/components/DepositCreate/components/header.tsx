import { Box, Divider, Text, Title } from '@mantine/core'

export default function () {

  return (
    <Box mb={20}>
      <Title order={2} mb={15}>New Deposit</Title>
      <Text>Create a new deposit to fund a contract.</Text>
      <Divider mt={10} />
    </Box>
  )
}

