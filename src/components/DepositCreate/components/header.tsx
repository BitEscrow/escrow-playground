import { Box, Divider, Text, Title } from '@mantine/core'

export default function () {

  return (
      <Box>
        <Title order={2} mb={15}>New Deposit</Title>
        <Text>Create a new deposit for funding a contract.</Text>
        <Divider mt={10} />
    </Box>
  )
}

